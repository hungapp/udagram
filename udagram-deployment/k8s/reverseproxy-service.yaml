apiVersion: v1
kind: Service
metadata:
  name: reverse-proxy-svc
spec:
  type: LoadBalancer
  selector:
    service: reverse-proxy
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
